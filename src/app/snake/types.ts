export interface SnakeSegment {
  x: number; // Floating point position for smooth movement
  y: number; // Floating point position for smooth movement
}

export type { SnakeTypeId } from "./snake-types";
